import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.ticketone.it', 'images.unsplash.com', 'tasterplace.com', 'artemedialab.it', 'www.artemedialab.it'],
  },
};

export default nextConfig;
